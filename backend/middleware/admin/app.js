const sql = require("../../database");
const nodemailer = require("nodemailer");

const selectTeam = (id, res) => {};
const rejectTeam = (id, res) => {};

module.exports = { selectTeam, rejectTeam };
